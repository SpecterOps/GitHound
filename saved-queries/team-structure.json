{
  "name": "Team Structure",
  "query": "MATCH p=(:GH_User)-[:GH_HasRole]->(:GH_TeamRole)-[:GH_MemberOf*1..]->(:GH_Team) RETURN p",
  "description": "Returns the structure of teams within organizations, including team roles and their members."
}