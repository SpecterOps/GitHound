{
  "name": "GitHub-to-Azure Identity Assumptions",
  "query": "MATCH p=(src)-[:CanAssumeIdentity]->(cred:AZFederatedIdentityCredential) RETURN p",
  "description": "Finds GitHub entities (repositories, branches, environments) that can assume Azure identities via OIDC federation. Verify that each trust relationship is intentional and scoped appropriately."
}
