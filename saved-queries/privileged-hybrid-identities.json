{
  "name": "Privileged Hybrid Identities",
  "query": "MATCH p=()-[:SyncedToGHUser]->(:GH_User)-[:GH_HasRole]->(:GH_OrgRole {short_name:'owners'}) RETURN p",
  "description": "Returns all hybrid identities (e.g., Azure or Okta users) that are associated with GitHub users who hold the organization owners role."
}